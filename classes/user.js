export default class User {
    constructor(firstName, lastName, password, age, gender, postCount, userId) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.password = password;
        this.age = age;
        this.gender = gender;
        this.postCount = postCount
        this.userId = userId
    }
}